export const ChildComponentOne = ({ name, age }) => {
    return (
        <div>
            <h1>Child Component One ... </h1>
            <p>Name is : {name} from child component One</p>
            <p>Age is : {age}</p>
        </div>
    );
}
